{
  "name": "fast-review",
  "version": "0.1.0",
  "description": "AI code review in under 60 seconds",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --minify",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest"
  },
  "keywords": [
    "github-action",
    "code-review",
    "ai",
    "claude",
    "security"
  ],
  "author": "Tunajam",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3",
    "vitest": "^2.0.0"
  }
}
